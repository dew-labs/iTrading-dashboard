// Products Feature - API Hooks
import { useProducts } from '../../../hooks/useProducts'
import { useProductsFiltering } from '../../../hooks/useProductsFiltering'

// Re-export existing hooks for backward compatibility
export { useProducts, useProductsFiltering }
